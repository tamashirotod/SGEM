import 'package:flutter/material.dart';

class ConsultTrainingPage extends StatelessWidget {
  const ConsultTrainingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Contenido de Consultar Entrenamiento'),
      ),
    );
  }
}
